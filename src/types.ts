export interface Listing {
  id: string;
  title: string;
  farmer: string;
  category: string;
  price: string;
  location: string;
  contact: string;
  imageUrl: string;
  createdAt: string;
  owner?: string;
}
